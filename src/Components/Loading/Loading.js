import React from 'react';

const Loading = () => {
    return (
        <div className='max-h-screen flex justify-center items-center'>
            <button className="btn loading m-6">loading</button>
        </div>
    )
};

export default Loading;